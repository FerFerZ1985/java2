const persona = {
    nombre: 'Jorge',
    edad: 35,
    canal: 'Codigo Creativo',
    curso: 'JavaScript desde cero',
    holaMundo: () => {
        console.log('Hola Mundo!');
    }
};
persona.holaMundo()
// Metodos personalizados - Metodo creado en el objeto

// Object.keys() - Devuelve un arreglo con las propiedades del objeto
// const resultado = Object.keys(persona);
// console.log(resultado);

// Ovbject.values() - Devuelve un arreglo con os valores del obejto
// const resultado = Object.values(persona);
// console.log(resultado);

// Object.entries() - Devielve un arreglo con propiedad y valor del objeto
// const resultado = Object.entries(persona);
// console.log(resultado);